<ul>
  {{#each albums}}
    <li>

      {{#if images.length}}
        <img src='{{images.0.url}}' alt='{{name}}' />
        <a href='/tracks/{{id}}'>{{name}}</a>
      {{/if}}
    </li>
  {{/each}}
</ul>